#!/usr/bin/env bash
# Wrapper que corre el CLI propio del proyecto.
# Uso:
#   ./so101 follower move --pose home
#   ./so101 leader record-trajectory traj.json
set -euo pipefail
HERE="$(cd "$(dirname "$0")" && pwd)"
exec "$HERE/.venv/bin/python" -m so101_cli "$@"
